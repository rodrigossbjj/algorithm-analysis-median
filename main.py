"""Ponto de entrada para executar o benchmark e gerar o gráfico."""
import plot_results  # when imported, it runs the benchmark and shows/saves the plot

if __name__ == "__main__":
    # importing the module executed the work; nothing else to do here.
    pass
